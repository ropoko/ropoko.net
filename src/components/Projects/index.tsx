import { ProjectsStyle } from './ProjectsStyle';

const Projects = () => {
	return (
		<ProjectsStyle>
			<h1>work in progress...</h1>
			<progress></progress>
		</ProjectsStyle>
	);
};

export default Projects;
